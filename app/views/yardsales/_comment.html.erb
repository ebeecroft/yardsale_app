<span class="description">Comment: <%= comment.comment %></span>
<span class="timestamp">Posted <%= time_ago_in_words(comment.created_at) %> ago.</span>

  <% if current_user?(comment.yardsale) %> |
    <% @user ||= current_user %>
    <%= link_to "delete", yardsale_comment_path(@yardsale, comment), method: :delete, data: { confirm: "You sure?" } %>
  <% end %>